// Client-side JS for Domain Watch UI — served at /app.js
// CSRF token is injected inline before this script loads

export const appJs: string = "\n// \u2500\u2500 API \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nasync function api(method, path, body) {\n  const headers = {'Content-Type':'application/json','X-Requested-With':'XMLHttpRequest'};\n  if (CSRF && method !== 'GET') headers['X-CSRF-Token'] = CSRF;\n  const r = await fetch(path, {method, headers, body: body ? JSON.stringify(body) : undefined});\n  if (r.status === 401) { window.location.href = '/'; return null; }\n  const data = await r.json().catch(() => ({}));\n  if (!r.ok) throw new Error(data.error || 'Request failed (' + r.status + ')');\n  return data;\n}\n\n// \u2500\u2500 Tabs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfunction switchTab(name) {\n  document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));\n  document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));\n  document.getElementById('tab-' + name).classList.add('active');\n  document.getElementById('panel-' + name).classList.add('active');\n  if (name === 'alerts') loadAlerts();\n  if (name === 'settings') loadSettings();\n  if (name === 'fuzzy') loadFuzzyHistory();\n}\n\n// \u2500\u2500 Toast \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfunction toast(msg, type) {\n  type = type || 'success';\n  const w = document.getElementById('toastWrap');\n  const t = document.createElement('div');\n  t.className = 'toast toast-' + type;\n  t.textContent = msg;\n  w.appendChild(t);\n  setTimeout(() => t.remove(), 5000);\n}\n\n// \u2500\u2500 Logout \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nasync function logout() {\n  await fetch('/auth/logout', {method: 'POST'});\n  window.location.href = '/';\n}\n\n// \u2500\u2500 Threshold pills \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndocument.addEventListener('click', function(e) {\n  if (e.target.classList.contains('threshold-pill')) {\n    e.target.classList.toggle('selected');\n  }\n});\n\nfunction getSelectedThresholds(containerId) {\n  return Array.from(document.querySelectorAll('#' + containerId + ' .threshold-pill.selected'))\n    .map(function(el) { return parseInt(el.dataset.days); });\n}\n\n// \u2500\u2500 Monitor \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar allDomains = [];\nvar selectedDomainIds = new Set();\n\nasync function loadDomains() {\n  try {\n    var domains = await api('GET', '/api/domains');\n    if (!domains) return;\n    allDomains = domains;\n    renderDomains();\n    renderStats();\n  } catch(e) { toast('Failed to load domains: ' + e.message, 'error'); }\n}\n\nfunction renderStats() {\n  var total = allDomains.length;\n  var expired = allDomains.filter(function(d) { return d.expiresAt && daysLeft(d.expiresAt) <= 0; }).length;\n  var expiring = allDomains.filter(function(d) { return d.expiresAt && daysLeft(d.expiresAt) > 0 && daysLeft(d.expiresAt) <= 30; }).length;\n  var healthy = total - expired - expiring;\n  document.getElementById('monitorStats').innerHTML =\n    mkStat(total, 'Monitored', '') +\n    mkStat(healthy, 'Healthy', 'green') +\n    mkStat(expiring, 'Expiring soon', 'amber') +\n    mkStat(expired, 'Expired', 'red');\n}\n\nfunction mkStat(num, label, cls) {\n  return '<div class=\"stat\"><div class=\"stat-num ' + cls + '\">' + num + '</div><div class=\"stat-label\">' + label + '</div></div>';\n}\n\nfunction daysLeft(expiresAt) {\n  return Math.ceil((new Date(expiresAt) - Date.now()) / 86400000);\n}\n\nfunction esc(s) {\n  return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;');\n}\n\nfunction renderDomains() {\n  var list = document.getElementById('domainList');\n  if (!allDomains.length) {\n    list.innerHTML = '<div class=\"empty\"><div class=\"empty-icon\">&#x1F310;</div><p>No domains monitored yet.</p><p style=\"margin-top:8px;font-size:12px\">Add your first domain above.</p></div>';\n    return;\n  }\n  var sorted = allDomains.slice().sort(function(a, b) {\n    var da = a.expiresAt ? daysLeft(a.expiresAt) : 9999;\n    var db = b.expiresAt ? daysLeft(b.expiresAt) : 9999;\n    return da - db;\n  });\n  list.innerHTML = sorted.map(domainCard).join('');\n}\n\nfunction domainCard(d) {\n  var days = d.expiresAt ? daysLeft(d.expiresAt) : null;\n  var isExpired = days !== null && days <= 0;\n  var isExpiring = days !== null && days > 0 && days <= 30;\n  var iconClass = isExpired ? 'icon-red' : isExpiring ? 'icon-amber' : 'icon-green';\n  var cardStatus = isExpired ? 'status-expired' : isExpiring ? 'status-expiring' : '';\n  var isSelected = selectedDomainIds.has(d.id);\n  var id = d.id;\n\n  var badge = '';\n  if (days === null) badge = '<span class=\"badge badge-gray\">Expiry unknown</span>';\n  else if (isExpired) badge = '<span class=\"badge badge-red\">Expired ' + Math.abs(days) + 'd ago</span>';\n  else if (days <= 7) badge = '<span class=\"badge badge-red\">' + days + 'd left</span>';\n  else if (days <= 30) badge = '<span class=\"badge badge-amber\">' + days + 'd left</span>';\n  else badge = '<span class=\"badge badge-green\">' + days + 'd left</span>';\n\n  var expiry = d.expiresAt\n    ? new Date(d.expiresAt).toLocaleDateString('en-GB', {day:'numeric', month:'short', year:'numeric'})\n    : 'Expiry unavailable \\u2014 click refresh to retry';\n  var registrarHtml = d.registrar ? ' \\u00b7 ' + esc(d.registrar) : '';\n  var lastChecked = d.lastChecked\n    ? ' \\u00b7 Checked ' + new Date(d.lastChecked).toLocaleDateString('en-GB', {day:'numeric', month:'short'})\n    : '';\n\n  var allT = [90,60,30,14,7];\n  var pills = allT.map(function(t) {\n    return '<button class=\"threshold-pill' + (d.alertThresholds.indexOf(t) >= 0 ? ' selected' : '') +\n      '\" data-days=\"' + t + '\" type=\"button\">' + t + 'd</button>';\n  }).join('');\n\n  return [\n    '<div class=\"domain-card ' + cardStatus + (isSelected ? ' selected' : '') + '\" id=\"dc-' + id + '\" data-id=\"' + id + '\">',\n      '<input type=\"checkbox\" class=\"domain-checkbox\"' + (isSelected ? ' checked' : '') + ' data-action=\"select\" data-id=\"' + id + '\" title=\"Select\">',\n      '<div class=\"domain-icon ' + iconClass + '\">&#x1F310;</div>',\n      '<div class=\"domain-body\">',\n        '<div class=\"domain-name\">' + esc(d.domain) + '</div>',\n        '<div class=\"domain-meta\">' + (d.expiresAt ? 'Expires ' + expiry : expiry) + registrarHtml + lastChecked + '</div>',\n        '<div class=\"domain-thresholds\">',\n          '<span class=\"threshold-label\">Alert at:</span>',\n          badge,\n          '<button class=\"btn btn-sm\" style=\"font-size:11px;padding:3px 8px\" data-action=\"edit-thresholds\" data-id=\"' + id + '\" type=\"button\">Edit alerts</button>',\n        '</div>',\n        '<div class=\"threshold-editor\" id=\"te-' + id + '\">',\n          '<div style=\"font-size:12px;color:var(--text-muted);margin-bottom:6px\">Alert when domain expires within:</div>',\n          '<div class=\"threshold-pills\" id=\"tp-' + id + '\">' + pills + '</div>',\n          '<div style=\"margin-top:8px;display:flex;gap:6px\">',\n            '<button class=\"btn btn-sm btn-primary\" data-action=\"save-thresholds\" data-id=\"' + id + '\" type=\"button\">Save</button>',\n            '<button class=\"btn btn-sm\" data-action=\"cancel-thresholds\" data-id=\"' + id + '\" type=\"button\">Cancel</button>',\n          '</div>',\n        '</div>',\n      '</div>',\n      '<div class=\"domain-actions\">',\n        badge,\n        '<button class=\"btn-icon\" title=\"Refresh RDAP data\" data-action=\"refresh\" data-id=\"' + id + '\" type=\"button\">&#x21BB;</button>',\n        '<button class=\"btn-icon\" title=\"Fuzzy scan\" data-action=\"fuzzy\" data-id=\"' + id + '\" type=\"button\">&#x1F50D;</button>',\n        '<button class=\"btn-icon\" title=\"Delete\" data-action=\"delete\" data-id=\"' + id + '\" type=\"button\">&#x1F5D1;</button>',\n      '</div>',\n    '</div>'\n  ].join('');\n}\n\n// Event delegation for domain card actions\ndocument.addEventListener('click', function(e) {\n  var btn = e.target.closest('[data-action]');\n  if (!btn) return;\n  var action = btn.dataset.action;\n  var id = btn.dataset.id;\n\n  // Threshold pills handled separately\n  if (btn.classList.contains('threshold-pill')) return;\n\n  if (action === 'select') {\n    toggleDomainSelect(id, btn.checked);\n  } else if (action === 'edit-thresholds') {\n    var el = document.getElementById('te-' + id);\n    if (el) el.classList.toggle('open');\n  } else if (action === 'cancel-thresholds') {\n    var el2 = document.getElementById('te-' + id);\n    if (el2) el2.classList.remove('open');\n  } else if (action === 'save-thresholds') {\n    saveThresholds(id);\n  } else if (action === 'refresh') {\n    refreshDomain(id, btn);\n  } else if (action === 'fuzzy') {\n    var dom = allDomains.find(function(d) { return d.id === id; });\n    if (dom) quickFuzzyScan(dom.domain);\n  } else if (action === 'delete') {\n    deleteDomain(id);\n  } else if (action === 'rescan') {\n    var domain = btn.dataset.domain;\n    if (domain) { document.getElementById('fuzzyInput').value = domain; runFuzzyScan(); }\n  } else if (action === 'rescan-unknowns') {\n    rescanUnknowns(btn.dataset.scanid);\n  } else if (action === 'delete-scan') {\n    deleteScan(btn.dataset.scanid);\n  } else if (action === 'show-scan') {\n    showFuzzyScan(btn.dataset.scanid);\n  }\n});\n\nasync function saveThresholds(id) {\n  var thresholds = getSelectedThresholds('tp-' + id);\n  try {\n    var updated = await api('PUT', '/api/domains/' + id + '/thresholds', {thresholds: thresholds});\n    if (!updated) return;\n    allDomains = allDomains.map(function(d) { return d.id === id ? updated : d; });\n    renderDomains();\n    toast('Alert thresholds updated');\n  } catch(e) { toast(e.message, 'error'); }\n}\n\nfunction toggleDomainSelect(id, checked) {\n  if (checked) selectedDomainIds.add(id);\n  else selectedDomainIds.delete(id);\n  var card = document.getElementById('dc-' + id);\n  if (card) card.classList.toggle('selected', checked);\n  updateBulkBar();\n}\n\nfunction toggleSelectAll(checked) {\n  allDomains.forEach(function(d) {\n    if (checked) selectedDomainIds.add(d.id);\n    else selectedDomainIds.delete(d.id);\n  });\n  document.querySelectorAll('.domain-checkbox').forEach(function(cb) { cb.checked = checked; });\n  document.querySelectorAll('.domain-card').forEach(function(c) { c.classList.toggle('selected', checked); });\n  updateBulkBar();\n}\n\nfunction clearDomainSelection() {\n  selectedDomainIds.clear();\n  document.querySelectorAll('.domain-checkbox').forEach(function(cb) { cb.checked = false; });\n  document.querySelectorAll('.domain-card').forEach(function(c) { c.classList.remove('selected'); });\n  document.getElementById('selectAllDomains').checked = false;\n  updateBulkBar();\n}\n\nfunction updateBulkBar() {\n  var bar = document.getElementById('domainBulkBar');\n  var count = selectedDomainIds.size;\n  bar.classList.toggle('visible', count > 0);\n  document.getElementById('domainBulkCount').textContent = count + ' selected';\n}\n\nasync function addDomain() {\n  var input = document.getElementById('addDomainInput');\n  var domain = input.value.trim();\n  if (!domain) return;\n  var thresholds = getSelectedThresholds('addThresholds');\n  var btn = document.getElementById('addDomainBtn');\n  var spinner = document.getElementById('addDomainSpinner');\n  btn.disabled = true;\n  spinner.classList.remove('hidden');\n  try {\n    var result = await api('POST', '/api/domains', {domain: domain, alertThresholds: thresholds});\n    if (!result) return;\n    allDomains.push(result);\n    renderDomains();\n    renderStats();\n    input.value = '';\n    if (!result.expiresAt) {\n      toast('Domain added. Expiry data unavailable \\u2014 RDAP lookup failed for this domain.', 'error');\n    } else {\n      toast('Domain added and checked');\n    }\n  } catch(e) { toast(e.message, 'error'); }\n  btn.disabled = false;\n  spinner.classList.add('hidden');\n}\n\nasync function deleteDomain(id) {\n  if (!confirm('Remove this domain from monitoring?')) return;\n  try {\n    await api('DELETE', '/api/domains/' + id);\n    allDomains = allDomains.filter(function(d) { return d.id !== id; });\n    selectedDomainIds.delete(id);\n    renderDomains();\n    renderStats();\n    updateBulkBar();\n    toast('Domain removed');\n  } catch(e) { toast(e.message, 'error'); }\n}\n\nasync function bulkDeleteDomains() {\n  var ids = Array.from(selectedDomainIds);\n  if (!ids.length) return;\n  if (!confirm('Delete ' + ids.length + ' domain(s) from monitoring?')) return;\n  try {\n    await api('POST', '/api/domains/bulk-delete', {ids: ids});\n    allDomains = allDomains.filter(function(d) { return ids.indexOf(d.id) < 0; });\n    selectedDomainIds.clear();\n    renderDomains();\n    renderStats();\n    updateBulkBar();\n    document.getElementById('selectAllDomains').checked = false;\n    toast(ids.length + ' domain(s) deleted');\n  } catch(e) { toast(e.message, 'error'); }\n}\n\nasync function refreshDomain(id, btn) {\n  if (btn) btn.disabled = true;\n  setTimeout(async function() {\n    try {\n      var updated = await api('POST', '/api/domains/' + id + '/refresh');\n      if (!updated) return;\n      allDomains = allDomains.map(function(d) { return d.id === id ? updated : d; });\n      renderDomains();\n      renderStats();\n      toast(updated.expiresAt ? 'Refreshed \\u2014 expires ' + new Date(updated.expiresAt).toLocaleDateString('en-GB', {day:'numeric',month:'short',year:'numeric'}) : 'Refresh complete \\u2014 RDAP returned no expiry data (check Worker logs)');\n    } catch(e) { toast(e.message, 'error'); }\n    if (btn) btn.disabled = false;\n  }, 0);\n}\n\nfunction quickFuzzyScan(domain) {\n  switchTab('fuzzy');\n  document.getElementById('fuzzyInput').value = domain;\n  runFuzzyScan();\n}\n\nfunction openBulkModal() { document.getElementById('bulkModal').classList.remove('hidden'); }\nfunction closeBulkModal() { document.getElementById('bulkModal').classList.add('hidden'); }\n\nasync function submitBulkAdd() {\n  var raw = document.getElementById('bulkDomainsInput').value;\n  var domains = raw.split('\\n').map(function(s) { return s.trim(); }).filter(Boolean);\n  if (!domains.length) return;\n  var thresholds = getSelectedThresholds('bulkThresholds');\n  try {\n    var results = await api('POST', '/api/domains/bulk-monitor', {domains: domains, alertThresholds: thresholds});\n    if (!results) return;\n    var added = results.filter(function(r) { return r.status === 'added'; }).length;\n    closeBulkModal();\n    await loadDomains();\n    toast(added + ' domain(s) added' + (results.length > added ? ', some skipped' : ''));\n    document.getElementById('bulkDomainsInput').value = '';\n  } catch(e) { toast(e.message, 'error'); }\n}\n\n// \u2500\u2500 Fuzzy Finder \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar fuzzyScans = [];\nvar currentScanId = null;\n\nasync function loadFuzzyHistory() {\n  try {\n    var scans = await api('GET', '/api/fuzzy');\n    if (!scans) return;\n    fuzzyScans = scans;\n    renderFuzzyHistory();\n    if (currentScanId) {\n      var scan = fuzzyScans.find(function(s) { return s.id === currentScanId; });\n      if (scan) renderFuzzyResults(scan);\n    }\n  } catch(e) { toast('Failed to load scan history', 'error'); }\n}\n\nfunction renderFuzzyHistory() {\n  var el = document.getElementById('fuzzyHistory');\n  var countEl = document.getElementById('fuzzyScanCount');\n  countEl.textContent = fuzzyScans.length + ' scan' + (fuzzyScans.length === 1 ? '' : 's');\n  if (!fuzzyScans.length) {\n    el.innerHTML = '<div class=\"empty\"><div class=\"empty-icon\">&#x1F50D;</div><p>No scans yet.</p></div>';\n    return;\n  }\n  el.innerHTML = fuzzyScans.map(function(s) {\n    var reg = s.results.filter(function(r) { return r.registered === true; }).length;\n    var avail = s.results.filter(function(r) { return r.registered === false; }).length;\n    var unk = s.results.filter(function(r) { return r.registered === null; }).length;\n    var date = new Date(s.scannedAt).toLocaleDateString('en-GB', {day:'numeric', month:'short', year:'numeric', hour:'2-digit', minute:'2-digit'});\n    return [\n      '<div class=\"scan-item\" data-action=\"show-scan\" data-scanid=\"' + s.id + '\">',\n        '<span>&#x1F50D;</span>',\n        '<span class=\"scan-domain\">' + esc(s.baseDomain) + '</span>',\n        '<span class=\"badge badge-red\" style=\"font-size:11px\">' + reg + ' taken</span>',\n        '<span class=\"badge badge-green\" style=\"font-size:11px\">' + avail + ' free</span>',\n        unk ? '<span class=\"badge badge-gray\" style=\"font-size:11px\">' + unk + ' unknown</span>' : '',\n        '<span class=\"scan-meta\">' + date + '</span>',\n        unk ? '<button class=\"btn-icon\" title=\"Recheck unknowns only\" data-action=\"rescan-unknowns\" data-scanid=\"' + s.id + '\" type=\"button\" style=\"font-size:12px\">?&#x21BB;</button>' : '',\n        '<button class=\"btn-icon\" title=\"Full rescan\" data-action=\"rescan\" data-domain=\"' + esc(s.baseDomain) + '\" type=\"button\">&#x21BB;</button>',\n        '<button class=\"btn-icon\" title=\"Delete scan\" data-action=\"delete-scan\" data-scanid=\"' + s.id + '\" type=\"button\">&#x1F5D1;</button>',\n      '</div>'\n    ].join('');\n  }).join('');\n}\n\nasync function deleteScan(id) {\n  if (!confirm('Delete this scan from history?')) return;\n  try {\n    await api('DELETE', '/api/fuzzy/' + id);\n    fuzzyScans = fuzzyScans.filter(function(s) { return s.id !== id; });\n    if (currentScanId === id) {\n      currentScanId = null;\n      document.getElementById('fuzzyResults').innerHTML = '';\n    }\n    renderFuzzyHistory();\n    toast('Scan deleted');\n  } catch(e) { toast(e.message, 'error'); }\n}\n\nasync function rescanUnknowns(id) {\n  var btn = document.querySelector('[data-action=\"rescan-unknowns\"][data-scanid=\"' + id + '\"]');\n  if (btn) btn.disabled = true;\n  toast('Rechecking unknown domains...');\n  setTimeout(async function() {\n    try {\n      var updated = await api('POST', '/api/fuzzy/' + id + '/rescan-unknowns');\n      if (!updated) return;\n      fuzzyScans = fuzzyScans.map(function(s) { return s.id === updated.id ? updated : s; });\n      renderFuzzyHistory();\n      if (currentScanId === id) renderFuzzyResults(updated);\n      var stillUnknown = updated.results.filter(function(r) { return r.registered === null; }).length;\n      toast('Recheck complete' + (stillUnknown ? ' \\u2014 ' + stillUnknown + ' still unknown (DNS errors)' : ' \\u2014 all resolved'));\n    } catch(e) { toast(e.message, 'error'); }\n    if (btn) btn.disabled = false;\n  }, 0);\n}\n\nfunction showFuzzyScan(id) {\n  var scan = fuzzyScans.find(function(s) { return s.id === id; });\n  if (!scan) return;\n  currentScanId = id;\n  renderFuzzyResults(scan);\n  document.getElementById('fuzzyInput').value = scan.baseDomain;\n  window.scrollTo({top: 0, behavior: 'smooth'});\n}\n\nasync function runFuzzyScan() {\n  var domain = document.getElementById('fuzzyInput').value.trim();\n  if (!domain) return;\n  var btn = document.getElementById('fuzzyScanBtn');\n  var spinner = document.getElementById('fuzzySpinner');\n  btn.disabled = true;\n  spinner.classList.remove('hidden');\n  // Yield to browser before starting heavy async work\n  await new Promise(function(r) { setTimeout(r, 0); });\n  document.getElementById('fuzzyResults').innerHTML =\n    '<div style=\"text-align:center;padding:40px;color:var(--text-muted)\">' +\n    '<div class=\"spinner spinner-dark\" style=\"width:24px;height:24px;margin:0 auto 14px\"></div>' +\n    '<p style=\"font-size:13px\">Scanning DNS variants of <strong>' + esc(domain) + '</strong></p>' +\n    '<p style=\"font-size:12px;margin-top:6px;color:var(--text-hint)\">Checking TLDs and typo variants \\u2014 may take 20\\u201340 seconds...</p>' +\n    '</div>';\n  try {\n    var scan = await api('POST', '/api/fuzzy', {domain: domain});\n    if (!scan) return;\n    currentScanId = scan.id;\n    // Replace or prepend \u2014 server already deduplicates by domain\n    var idx = fuzzyScans.findIndex(function(s) { return s.baseDomain === scan.baseDomain; });\n    if (idx >= 0) fuzzyScans[idx] = scan;\n    else fuzzyScans = [scan].concat(fuzzyScans);\n    renderFuzzyHistory();\n    renderFuzzyResults(scan);\n  } catch(e) {\n    toast(e.message, 'error');\n    document.getElementById('fuzzyResults').innerHTML = '';\n  }\n  btn.disabled = false;\n  spinner.classList.add('hidden');\n}\n\nfunction renderFuzzyResults(scan) {\n  var registered = scan.results.filter(function(r) { return r.registered === true; });\n  var available = scan.results.filter(function(r) { return r.registered === false; });\n  var unknown = scan.results.filter(function(r) { return r.registered === null; });\n  var date = new Date(scan.scannedAt).toLocaleString('en-GB');\n\n  var typeLabel = function(t) {\n    return {tld:'TLD','typo-swap':'swap','typo-drop':'drop','typo-double':'double','typo-hyphen':'hyphen','typo-homoglyph':'homoglyph'}[t] || t;\n  };\n\n  function renderGroup(items, cls) {\n    return items.map(function(v) {\n      return '<div class=\"fuzzy-item ' + cls + '\">' +\n        '<div class=\"fi-top\"><span class=\"fi-name\" title=\"' + esc(v.domain) + '\">' + esc(v.domain) + '</span>' +\n        '<span class=\"dot ' + (cls === 'registered' ? 'dot-red' : cls === 'available' ? 'dot-green' : 'dot-gray') + '\"></span></div>' +\n        '<div class=\"fi-tags\"><span class=\"fi-tag ' + (v.type === 'tld' ? 'tag-tld' : 'tag-typo') + '\">' + typeLabel(v.type) + '</span></div>' +\n        '</div>';\n    }).join('');\n  }\n\n  var unknownSection = '';\n  if (unknown.length) {\n    unknownSection = '<div style=\"font-size:11px;font-weight:600;color:var(--text-muted);margin:14px 0 4px;text-transform:uppercase;letter-spacing:0.06em\">' +\n      'Unknown (' + unknown.length + ') \\u2014 ' +\n      '<button class=\"btn btn-sm\" style=\"font-size:11px\" data-action=\"rescan-unknowns\" data-scanid=\"' + scan.id + '\" type=\"button\">Recheck these only</button>' +\n      '</div>' +\n      '<div class=\"fuzzy-grid\">' + renderGroup(unknown, '') + '</div>';\n  }\n\n  document.getElementById('fuzzyResults').innerHTML =\n    '<div class=\"card\">' +\n    '<div style=\"display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;flex-wrap:wrap;gap:10px\">' +\n      '<div>' +\n        '<span style=\"font-size:15px;font-weight:600\">' + esc(scan.baseDomain) + '</span>' +\n        '<span style=\"font-size:12px;color:var(--text-muted);margin-left:10px\">scanned ' + date + '</span>' +\n      '</div>' +\n      '<div style=\"display:flex;gap:8px;flex-wrap:wrap;align-items:center\">' +\n        '<span class=\"badge badge-red\">' + registered.length + ' taken</span>' +\n        '<span class=\"badge badge-green\">' + available.length + ' free</span>' +\n        (unknown.length ? '<span class=\"badge badge-gray\">' + unknown.length + ' unknown</span>' : '') +\n        '<button class=\"btn btn-sm\" data-action=\"rescan\" data-domain=\"' + esc(scan.baseDomain) + '\" type=\"button\">&#x21BB; Full rescan</button>' +\n      '</div>' +\n    '</div>' +\n    (registered.length ? '<div style=\"font-size:11px;font-weight:600;color:var(--text-muted);margin-bottom:6px;text-transform:uppercase;letter-spacing:0.06em\">Registered (taken)</div><div class=\"fuzzy-grid\">' + renderGroup(registered, 'registered') + '</div>' : '') +\n    (available.length ? '<div style=\"font-size:11px;font-weight:600;color:var(--text-muted);margin:14px 0 6px;text-transform:uppercase;letter-spacing:0.06em\">Available</div><div class=\"fuzzy-grid\">' + renderGroup(available, 'available') + '</div>' : '') +\n    unknownSection +\n    '<div class=\"legend\">' +\n      '<div class=\"leg-item\"><span class=\"dot dot-red\"></span> Taken</div>' +\n      '<div class=\"leg-item\"><span class=\"dot dot-green\"></span> Available</div>' +\n      '<div class=\"leg-item\"><span class=\"dot dot-gray\"></span> Unknown (DNS error)</div>' +\n    '</div>' +\n    '</div>';\n}\n\n// \u2500\u2500 Alerts \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nasync function loadAlerts() {\n  try {\n    var alerts = await api('GET', '/api/alerts');\n    if (!alerts) return;\n    var el = document.getElementById('alertsList');\n    if (!alerts.length) {\n      el.innerHTML = '<div class=\"empty\"><div class=\"empty-icon\">&#x1F514;</div><p>No alerts sent yet.</p></div>';\n      return;\n    }\n    el.innerHTML = alerts.map(function(a) {\n      var icon = a.type === 'expired' ? '&#x1F6AB;' : a.daysRemaining <= 7 ? '&#x26A0;' : '&#x1F514;';\n      var badgeCls = a.type === 'expired' ? 'badge-red' : a.daysRemaining <= 14 ? 'badge-amber' : 'badge-green';\n      var msg = a.type === 'expired' ? 'Domain has expired' : 'Expires in ' + a.daysRemaining + ' days';\n      var date = new Date(a.sentAt).toLocaleString('en-GB', {day:'numeric', month:'short', hour:'2-digit', minute:'2-digit'});\n      return '<div class=\"alert-item\">' +\n        '<div class=\"alert-icon\">' + icon + '</div>' +\n        '<div class=\"alert-body\">' +\n          '<div class=\"alert-domain\">' + esc(a.domain) + '</div>' +\n          '<div class=\"alert-msg\">' + msg + ' \\u00b7 via ' + esc(a.emailProvider) + ' \\u00b7 ' + (a.success ? '\\u2714 sent' : '\\u2716 failed') + '</div>' +\n        '</div>' +\n        '<div style=\"display:flex;flex-direction:column;align-items:flex-end;gap:4px\">' +\n          '<span class=\"badge ' + badgeCls + '\" style=\"font-size:11px\">' + (a.type === 'expired' ? 'expired' : a.daysRemaining + 'd') + '</span>' +\n          '<span class=\"alert-time\">' + date + '</span>' +\n        '</div>' +\n        '</div>';\n    }).join('');\n  } catch(e) { toast('Failed to load alerts', 'error'); }\n}\n\n// \u2500\u2500 Settings \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nasync function loadSettings() {\n  try {\n    var s = await api('GET', '/api/settings');\n    if (!s) return;\n    document.getElementById('emailFrom').value = s.emailFrom || '';\n    document.getElementById('emailTo').value = s.emailTo || '';\n    document.getElementById('subjectPrefix').value = s.emailSubjectPrefix || '';\n\n    // Provider dots \u2014 show all 3 providers with configured status\n    var providerNames = {resend:'Resend', mailgun:'Mailgun', sendgrid:'SendGrid'};\n    var isActive = s.availableProviders && s.availableProviders.length > 0;\n\n    // All 3 providers with green/grey dots\n    var providerRows = (s.providerStatus || []).map(function(p) {\n      return '<div style=\"display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:0.5px solid var(--border)\">' +\n        '<span class=\"provider-dot' + (p.configured ? ' active' : '') + '\" style=\"flex-shrink:0\"></span>' +\n        '<span style=\"flex:1;font-size:13px;font-weight:500\">' + (providerNames[p.id] || p.id) + '</span>' +\n        '<span style=\"font-size:12px;color:' + (p.configured ? 'var(--green)' : 'var(--text-hint)') + '\">' +\n          (p.configured ? 'Configured' : 'Not configured') +\n        '</span>' +\n      '</div>';\n    }).join('');\n    document.getElementById('providerInfo').innerHTML = providerRows ||\n      '<div style=\"font-size:13px;color:var(--text-muted)\">No providers detected \u2014 set an API key secret in Cloudflare.</div>';\n\n    // Dropdown: only configured providers\n    var sel = document.getElementById('providerSelect');\n    sel.innerHTML = '';\n    if (!isActive) {\n      sel.innerHTML = '<option value=\"\">No providers configured</option>';\n      sel.disabled = true;\n    } else {\n      s.availableProviders.forEach(function(p) {\n        var opt = document.createElement('option');\n        opt.value = p;\n        opt.textContent = providerNames[p] || p;\n        if (p === s.emailProvider) opt.selected = true;\n        sel.appendChild(opt);\n      });\n      sel.disabled = false;\n    }\n\n    document.getElementById('providerStatusBadge').textContent = isActive ? ('Email: ' + (providerNames[s.emailProvider] || s.emailProvider)) : 'No email';\n    document.getElementById('providerStatusBadge').className = 'badge ' + (isActive ? 'badge-green' : 'badge-gray');\n\n    document.getElementById('limitsDisplay').innerHTML = [\n      ['Max monitored domains', s.maxMonitoredDomains],\n      ['Max fuzzy scan history', s.maxFuzzyHistory],\n      ['Default alert thresholds', s.defaultThresholds + ' days'],\n      ['Fuzzy TLDs checked', s.fuzzyTlds],\n    ].map(function(kv) { return '<div><strong>' + kv[0] + ':</strong> ' + esc(String(kv[1])) + '</div>'; }).join('');\n  } catch(e) { toast('Failed to load settings', 'error'); }\n}\n\nasync function saveSettings() {\n  var provider = document.getElementById('providerSelect').value;\n  try {\n    await api('PUT', '/api/settings', {\n      emailFrom: document.getElementById('emailFrom').value,\n      emailTo: document.getElementById('emailTo').value,\n      emailSubjectPrefix: document.getElementById('subjectPrefix').value,\n      emailProvider: provider,\n    });\n    toast('Settings saved');\n    loadSettings();\n  } catch(e) { toast(e.message, 'error'); }\n}\n\nasync function sendTestEmail() {\n  var provider = document.getElementById('providerSelect').value;\n  var btn = document.getElementById('testEmailBtn');\n  btn.disabled = true;\n  btn.textContent = 'Sending...';\n  try {\n    var r = await api('POST', '/api/settings/test-email', {provider: provider});\n    if (r) toast('Test email sent via ' + r.provider + ' \\u2014 check your inbox');\n  } catch(e) { toast(e.message, 'error'); }\n  btn.disabled = false;\n  btn.textContent = 'Send test email';\n}\n\n// \u2500\u2500 Init \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndocument.getElementById('addDomainInput').addEventListener('keydown', function(e) {\n  if (e.key === 'Enter') addDomain();\n});\ndocument.getElementById('fuzzyInput').addEventListener('keydown', function(e) {\n  if (e.key === 'Enter') runFuzzyScan();\n});\n\nloadDomains();\nloadSettings();\n";
